const numeros = [1, 2, 3, 4, 5];

const dobrado = numeros.map((num) =>  num * 2);

console.log(dobrado);

let nomes = ["Sofia", "Julia", "Larissa","Isabella","PA","João","Pablo"];

let mensagem = nomes.map((nome) => `Olá, ${nome}`);

console.log(mensagem);
